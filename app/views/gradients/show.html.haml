.page-header
  %h1= @gradient.name

.panel.panel-default
  .panel-body{ style: @gradient.gradient.to_css + "height:100px;" }

.input-group
  = link_to gradient_path, method: :delete, class: "btn btn-danger" do
    %span.glyphicon.glyphicon-trash
    = "Remove gradient"